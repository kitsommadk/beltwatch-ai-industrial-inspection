# Operator impact

The operator-facing successful slit capture response remains Belt A + Belt B from one shared frame. The integrity improvement is mainly behind the console: the evidence ledger should no longer be able to show only one lane from a database write that failed halfway through a two-lane capture.

The frontend does not need a new interaction for this transaction change.