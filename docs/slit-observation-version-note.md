# Version note

The API version advances to `0.10.0` because slit auto-capture gains the additive `observation_id` field and a new `GET /api/slit-observations` endpoint. Existing successful single-belt response behavior remains unchanged.
