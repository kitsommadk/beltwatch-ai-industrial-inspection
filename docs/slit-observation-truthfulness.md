# Slit observation truthfulness boundary

The shared observation ledger records what the current software estimator observed in image coordinates. It does not convert pair diagnostics to inches, classify mechanical cause, or assert physical accuracy.

Width evidence remains independently calibrated per lane. Pair diagnostics stay in pixels until a physically validated cross-frame calibration model supports stronger units.
