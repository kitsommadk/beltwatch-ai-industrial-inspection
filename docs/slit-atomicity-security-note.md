# Atomicity and security

Atomic database writes improve evidence consistency but are not a security control by themselves. Authentication/RBAC, LAN HTTPS and outbound-only remote synchronization remain separate roadmap items for deployment hardening.