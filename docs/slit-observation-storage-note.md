# Storage behavior

Shared slit observations are stored in the same local SQLite database as sessions and evidence. No cloud service is required. Remote synchronization remains a future outbound-only deployment concern and is not introduced by this feature.
