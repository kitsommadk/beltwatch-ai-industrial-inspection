# Observation identity

Within one inspection session, `(camera_id, frame_sequence)` identifies the shared captured frame. The stored session ID completes the database uniqueness key. Belt A and Belt B evidence IDs then bind the shared observation to the exact independent measurements produced from that frame.
