# Portfolio wording

Acceptable after CI/merge: "Two-lane slit evidence is persisted atomically in SQLite so a shared frame cannot be represented by only one committed lane after a write failure."

Do not describe this as physical metrology validation, camera qualification or transactional audit logging.