# Slit pair invariant

For accepted automatic slit evidence:

`count(shared frame, expected lanes) == 2`

with exact lane identities `belt-a` and `belt-b`, matching camera/frame/position context, and independent measurement/temporal records. A failed database batch must leave the count at zero for that attempted observation.