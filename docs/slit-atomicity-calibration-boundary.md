# Calibration boundary

Each lane evidence record retains its calibration profile ID/version. Atomicity does not relax the temporal rule that comparisons are scoped to exact calibration identity, and it does not validate the calibration itself. Physical calibration remains a separate pilot gateway.