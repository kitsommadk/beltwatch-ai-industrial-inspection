# Machine-control boundary

The observation ledger is read/write inspection evidence storage only. It sends no command to the slitter, motor, PLC or encoder and does not change the project's observation/decision-support-only safety boundary.
