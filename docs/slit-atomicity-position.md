# Position semantics

Both lane evidence objects are generated from one sampled position before persistence. The atomic batch preserves that shared position in both lane rows. Position-source accuracy itself remains dependent on the configured simulated/software/read-only provider and future physical qualification.