# Query semantics

The pilot endpoint lists observations only for the most recently created/current session and orders them by footage position descending, then observation ID descending. It is intended for the active operator console, not cross-run analytics.

Cross-session comparison should be a separate explicit API with run/calibration comparability rules.
