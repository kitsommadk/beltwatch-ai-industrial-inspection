# Completion gateway

This branch is ready for PR review when its final head has a green repository CI run. If CI exposes test or schema issues, repair them before merge; do not weaken lane identity, shared-frame identity or truthfulness constraints to make tests pass.
