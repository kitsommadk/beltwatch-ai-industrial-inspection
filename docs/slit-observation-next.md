# Next engineering step

After the shared observation ledger is CI-validated, the next useful layer is temporal pair diagnostics: compare gap/center behavior against prior comparable observations from the same session, camera and calibration context while preserving explicit insufficient/incomparable states.

Do not infer root cause from those trends until physical slitter data supports validated rules.
