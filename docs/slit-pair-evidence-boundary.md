# Shared-frame evidence boundary

The atomic pair begins after the frame has been accepted by frame/geometry quality checks and converted into two lane evidence objects. It ends when both lane parent/child records commit. Response diagnostics and audit metadata are adjacent to, but outside, that transaction boundary.