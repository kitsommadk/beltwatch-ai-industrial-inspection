# Reliability boundary

The ledger uses local SQLite constraints and foreign keys for logical integrity. Power-loss, disk-full, filesystem and edge-PC restart testing remain separate deployment-reliability work and are not implied by repository-level tests.
