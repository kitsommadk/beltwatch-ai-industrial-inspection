# Frontend opportunity

The current console already shows latest shared-frame diagnostics. Once this ledger is merged, the frontend can add a small footage-position trend view for gap and lane centers using `/api/slit-observations`, clearly labeled as replay/pixel diagnostics until physical qualification.
