# Current limitation

A/B evidence is guaranteed all-or-nothing, but the new shared observation currently follows in a second transaction. That is acceptable for this branch as an explicitly documented pilot limitation, but the stronger evidence-plus-observation transaction is the recommended next integrity hardening before treating every slit evidence pair as guaranteed to have a ledger row.
