# API compatibility

For slit auto-capture, `observation_id` is additive; existing `run_layout`, `shared_frame_sequence`, `shared_position_ft`, `records` and `diagnostics` remain. Single auto-capture still returns the legacy raw evidence record.
