# CI gateway

Merge only after backend, optional-CV and frontend CI jobs pass on the final branch head. Backend coverage must include schema integrity, shared-frame persistence, duplicate rejection, session-scoped listing and single-belt compatibility.
