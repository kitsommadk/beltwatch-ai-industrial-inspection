# Footage position

The shared observation stores the same single position sample used by both lane evidence records. This gives future pair trends a footage axis while preserving the rule that position-source accuracy depends on the configured provider and still requires physical validation.
